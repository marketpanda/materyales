type TilesPriceType = {
    tiles?: number,
    grout?: number
}

export const priceMaterialsTiles: TilesPriceType = {
    tiles: 500,
    grout: 210
}

export const materialsTiles = {
    tiles: { price: 500, units: 'pcs' },
    grout: { price: 210, units: 'kg' },
}